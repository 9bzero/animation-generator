# animation-generator

Build CSS keyframe animations visually.

Select from common animation types (fade, slide, bounce, rotate, pulse, shake) or build a custom keyframe sequence. Preview in real time and copy the `@keyframes` + `animation` CSS.

## Run

```bash
npm install && npm run dev
```
